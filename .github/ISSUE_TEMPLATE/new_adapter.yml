name: New agent adapter
description: Propose a verified provider adapter
body:
  - type: input
    id: provider
    attributes: { label: Agent/provider }
    validations: { required: true }
  - type: textarea
    id: format
    attributes: { label: Public format documentation, description: Link public docs and sample schema without private sessions. }
    validations: { required: true }
