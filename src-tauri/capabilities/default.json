{
  "identifier": "main",
  "description": "Local application event access; no shell, HTTP or filesystem plugin access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "clipboard-manager:allow-write-text"
  ]
}
